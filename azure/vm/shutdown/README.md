# Azure VM Shutdown

Template for a controlled Azure VM shutdown experiment.

## Hypothesis

If a sandbox VM is shut down unexpectedly, the workload's monitoring, alerting and recovery process should behave as designed.

Use only a non-production VM and define the recovery procedure before starting.
